export interface Trip {
    key: string;
    name: string,
    description: string,
    price: number,
    available: number,
    capacity: number,
    destination: string,
    startDate: string,
    endDate: string,
    imageURL: string,
}
